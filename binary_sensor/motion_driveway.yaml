platform: template
sensors:
  motion_driveway:
    friendly_name: Driveway Camera
    device_class: motion
    entity_id: reokinkcamera.driveway
    value_template: "{{ is_state('reolinkcamera.driveway', 'motion') }}"
    delay_off:
      seconds: 30
