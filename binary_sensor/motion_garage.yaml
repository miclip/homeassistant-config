platform: template
sensors:
  motion_driveway:
    friendly_name: Garage Camera
    device_class: motion
    entity_id: reokinkcamera.garage
    value_template: "{{ is_state('reolinkcamera.garage', 'motion') }}"
    delay_off:
      seconds: 30
