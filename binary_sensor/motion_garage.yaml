platform: template
sensors:
  motion_garage:
    friendly_name: Garage Camera
    device_class: motion
    entity_id: camera.garage
    value_template: "{{ is_state('camera.garage', 'motion') }}"
    delay_off:
      seconds: 30
