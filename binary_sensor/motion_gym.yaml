platform: template
sensors:
  motion_gym:
    friendly_name: Basement Camera
    device_class: motion
    entity_id: camera.gym
    value_template: "{{ is_state('camera.gym', 'motion') }}"
    delay_off:
      seconds: 30
