db_url: !secret database_connection
purge_keep_days: 30
exclude:
  domains:
    - automation
    - updater
    - script
    - scene
  entity_globs:
    - sensor.sun_*
    - sensor.*_battery
  entities:
    - sun.sun
    - sensor.date
    - sensor.time
